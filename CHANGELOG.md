# Changelog

All notable changes to `rounddav_bookmarks` should be documented in this file.

## [Unreleased]

- Ongoing development builds use `rounddav_bookmarks::PLUGIN_VERSION` with a `+dev` suffix until the next release is cut.

## [1.0.1] - 2026-04-19

- Formalized the plugin's self-metadata through `rounddav_bookmarks::PLUGIN_VERSION` and `rounddav_bookmarks::info()`.
- Aligned self-versioning with a cleaner release workflow while keeping existing plugin behavior intact.

## [1.0.0] - 2026-04-11

- Formalized the plugin's self-metadata through `rounddav_bookmarks::PLUGIN_VERSION` and `rounddav_bookmarks::info()`.
- Aligned self-versioning with a cleaner release workflow while keeping existing plugin behavior intact.
